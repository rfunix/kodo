module generic_demo {
    meta { purpose: "Generic identity function", version: "0.1.0" }

    fn identity<T>(x: T) -> T { return x }

    fn main() -> Int {
        print_int(identity(42))
        print_int(identity(99))
        return 0
    }
}
