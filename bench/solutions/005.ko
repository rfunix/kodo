module fix_me {
    meta { purpose: "Fix missing return", version: "0.1.0" }

    fn add(a: Int, b: Int) -> Int {
        return a + b
    }

    fn main() -> Int {
        print_int(add(3, 4))
        println("")
        return 0
    }
}
