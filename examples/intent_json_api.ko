module intent_json_api {
    meta {
        purpose: "Demonstrates JSON API intent resolver",
        version: "1.0.0"
    }

    intent json_api {
        routes: ["/users", "/posts"],
        models: ["User", "Post"]
    }

    fn main() -> Int {
        println("intent json_api loaded")
        return 0
    }
}
