module math {
    meta {
        purpose: "Basic math utilities"
        version: "0.1.0"
    }

    pub fn add(a: Int, b: Int) -> Int {
        return a + b
    }

    pub fn multiply(a: Int, b: Int) -> Int {
        return a * b
    }
}
